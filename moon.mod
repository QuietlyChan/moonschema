name = "QuietlyChan/moonschema"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/QuietlyChan/moonschema"

license = "Apache-2.0"

keywords = [
  "json-schema",
  "validation",
  "schema",
  "api",
  "zod",
  "ajv",
  "wasm",
]

preferred_target = "wasm-gc"

description = "JSON Schema 2020-12 validator with a zod-style builder API for MoonBit — one engine, two frontends (ajv / zod), WASM-first."
